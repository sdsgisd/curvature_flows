//Define aliases for numeric.js and Math.js

var sqrt = Math.sqrt;
var pow = Math.pow;
var dot = numeric.dot;
var add = numeric.add;
var sub = numeric.sub;
var mul = numeric.mul;
var div = numeric.div;
var norm = numeric.norm2;
